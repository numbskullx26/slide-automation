type Props = {
  children: React.ReactNode;
  type: "FREE" | "PAID";
};

export const SubscriptionPlan = ({ children, type }: Props) => {
  //WIP: Return the subscription of the User
  return children;
};
